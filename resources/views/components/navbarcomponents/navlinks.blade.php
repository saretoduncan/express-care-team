<a {{ $attributes->merge(['class' => ' flex space-x-2 text-lg border-b pb-2 lg:border-none lg:pb-0']) }}" ">
    <i class="{{ $linkIcon }} self-center text-xl"></i>
    <p class="font-extrabold">{{ $linkName }}</p>

</a>
